import styled from "styled-components";

export const Spinner = styled.div`
  border: 50px solid var(--lightGrey);
  border-top: 5px solid var(--medGrey);
  border-radius: 50%;
  width: 50px;
  height: 50px;
  animation: spin;
`;
